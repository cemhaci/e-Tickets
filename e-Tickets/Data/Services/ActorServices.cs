using e_Tickets.Models;
using Microsoft.EntityFrameworkCore;

namespace e_Tickets.Data.Services
{
    public class ActorServices 
    {
        //AppDbContext _context;
        //public ActorServices(AppDbContext context)
        //{
        //    _context= context;
        //}
        //public void Add(Actor actor)
        //{
        //    _context.Actors.Add(actor);
        //    _context.SaveChanges();
        //}

        //public  void Delete(int id)
        //{
        //    //_context.Actors.FirstOrDefault(x=>x.)
        //    var get= GetById(id);
        //    _context.Actors.Remove(get);
        //    _context.SaveChanges();
        //}

        //public List<Actor> GetAll()
        //{
        //    var result=_context.Actors.ToList();
        //    return result;
        //}

        //public Actor GetById(int id)
        //{
        //    var actorid=_context.Actors.FirstOrDefault(x => x.id==id);
        //    return actorid;
        //}

        //public Actor Update(Actor newactor)
        //{
        //    _context.Update(newactor);
        //    _context.SaveChanges();
        //    return newactor;
        //}
    }
}
